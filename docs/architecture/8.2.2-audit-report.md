# Audit formale — Microstep 8.2.2 Priority, Ranking & Weighting Model

**Data audit:** verifica notarile su documento, tipi, aggregazione, ordinamento, kill-switch e test.  
**Obiettivo:** confermare assenza di semantica decisionale, rispetto dei contratti Fase 8 e assenza di comportamenti impliciti.

---

## §1 — Documento architetturale 8.2.2

**File verificato:** `docs/architecture/8.2.2-priority-ranking-model.md`

| Controllo | Esito | Note |
|-----------|--------|------|
| Stato VINCOLANTE | ✅ | §1: "VINCOLANTE", "MUST essere rispettato", "MUST NOT essere aggirato" |
| Freeze / chiusura | ✅ | §1 "Congelabile"; §8 "definitivo e congelato", "VINCOLANTE. CONGELABILE. NON ESTENDIBILE." |
| ranking ≠ decisione | ✅ | §2 "ranking descrittivo, MUST NOT prescrittivo"; §4 "Ranking ≠ decisione" |
| ordinamento ≠ selezione | ✅ | §4 "Ordinamento ≠ selezione"; §3 "MUST NOT filtrare o selezionare" |
| priority/weight NON risolvono conflitti | ✅ | §3 "MUST NOT semantica implicita"; §7 "VIETATO risolvere conflitti" |
| Assenza giustificazioni semantiche | ✅ (dopo correzione) | Rimossa formulazione "(priorità più alta prima)"; sostituita con "Convenzione meccanica dichiarata" |
| Assenza linguaggio orientato al risultato | ✅ (dopo correzione) | Aggiunta in §5: criterio trattato come convenzione meccanica, MUST NOT "importanza"/"migliore"/"vince" |
| Anti-pattern vietati | ✅ | §7 elenca esplicitamente risolvere conflitti, soglie, cutoff, selezione, semantica implicita, UX, side-effect |
| Dichiarazione finale di chiusura | ✅ | §8 formula notarile di freeze e vincolatività |

**Correzioni applicate:** (1) §3 SemanticPriority: "(priorità più alta prima)" sostituito con "Convenzione meccanica dichiarata: valore maggiore = posizione precedente". (2) §5: aggiunto punto esplicito che il criterio valore maggiore/minore = posizione è convenzione meccanica e MUST NOT essere giustificato semanticamente.

---

## §2 — Modello dei tipi (ranking / priority / weight)

**File verificati:** `src/semantic-layer/engine/Ranking.ts`, `OrderedView.ts` (solo uso tipi)

| Controllo | Esito | Note |
|-----------|--------|------|
| Tipi puramente dichiarativi | ✅ | SemanticRank, SemanticPriority, SemanticWeight = number; RankableSemanticState = interface con campi readonly opzionali |
| Senza metodi | ✅ | Nessun metodo nei tipi; OrderingOptions solo dati readonly |
| Senza logica incorporata nei tipi | ✅ | Nessuna funzione compare/isHigher/resolve nei tipi; logica di sort isolata in OrderedView |
| Valori come numeri opachi | ✅ | Nessuna interpretazione semantica nei tipi; commenti limitati a "posizione precedente" come convenzione |
| Nessun default "intelligente" | ✅ | Valori mancanti gestiti in OrderedView (sortKey) con Infinity/-Infinity per posizione deterministica, non per significato |

**Violazioni rilevate:** nessuna. La funzione sortKey in OrderedView è logica di presentazione (ordinamento meccanico), non parte del modello di tipi.

---

## §3 — SemanticModuleOutput

**File verificato:** `src/semantic-layer/engine/SemanticModuleOutput.ts`

| Controllo | Esito | Note |
|-----------|--------|------|
| Ranking/priority/weight opzionali | ✅ | RankableSemanticState ha rank?, priority?, weight? opzionali; states? opzionale |
| Output senza ranking = comportamento 8.2.1 | ✅ | SemanticState senza rank è compatibile con RankableSemanticState; aggregazione invariata; con ranking OFF engine restituisce snapshot da aggregate senza ordinamento |
| Nessuna validazione semantica sui valori | ✅ | isModuleOutputShape verifica solo forma (chiavi ammesse, valori array); nessun check su range o significato di rank/priority/weight |
| Forma validata, contenuto non interpretato | ✅ | Validazione: oggetto, chiavi in allowed set, valori array; nessuna interpretazione del contenuto degli array |

**Violazioni rilevate:** nessuna.

---

## §4 — Ordinamento nello snapshot

**File verificati:** `OrderedView.ts`, `SemanticEngine.ts`

| Controllo | Esito | Note |
|-----------|--------|------|
| Ordinamento deterministico | ✅ | Stesso snapshot + OrderingOptions → stesso risultato; sort stabile (return 0 su pari chiave) |
| Ordinamento meccanico e dichiarato | ✅ | sortKey puramente numerica; OrderingOptions dichiarate (orderBy, direction) |
| Nessun filtro/rimozione/sostituzione | ✅ | [...snapshot.states] poi sort; nessun filter/splice; tutti gli elementi restano |
| Tutti i contributi presenti | ✅ | length invariato; createOrderedView restituisce stesso numero di states |
| Criterio come convenzione meccanica | ✅ | Commenti "minore prima (asc)", "maggiore prima (asc)" senza "importanza" o "miglior risultato"; doc §5 esplicito |

**Violazioni rilevate:** nessuna.

---

## §5 — Kill-switch e degradazione

**File verificati:** `SemanticKillSwitch.ts`, `SemanticEngine.ts`

| Controllo | Esito | Note |
|-----------|--------|------|
| Kill-switch ranking OFF → nessun ordinamento | ✅ | if (!isRankingEnabled(this.killSwitch)) return snapshot; snapshot = output di aggregate (8.2.1) |
| Snapshot identico a 8.2.1 quando ranking OFF | ✅ | Restituito snapshot senza createOrderedView; ordine = ordine di aggregazione |
| Nessun side-effect sull’engine | ✅ | evaluate non modifica stato; nessuna scrittura esterna |
| Nessuna persistenza/caching del ranking | ✅ | createOrderedView crea nuovo snapshot a ogni chiamata; nessun cache di ordinamento |

**Violazioni rilevate:** nessuna.

---

## §6 — Test

**File verificato:** `src/semantic-layer/engine/tests/RankingAndOrderedView.spec.ts`

| Controllo | Esito | Note |
|-----------|--------|------|
| Ordine invariato senza ranking | ✅ | "contributi senza ranking → ordine originale"; createOrderedView su elementi senza rank non cambia ordine |
| Presenza ranking dichiarato → ordine esplicito | ✅ | "presenza di ranking dichiarato → elementi ordinati"; rank ascendente, priority "maggiore prima" come convenzione |
| Determinismo | ✅ | "stesso input → stesso ordine" |
| Nessuna selezione (tutti presenti) | ✅ | "nessuna selezione": tutti gli elementi restano presenti; length e set degli scope verificati |
| Kill-switch OFF → nessun ordinamento | ✅ | "ranking disabilitato: engine restituisce ordine 8.2.1 (aggregazione)" |
| Assenza linguaggio di selezione/decisione | ✅ | Nessuna occorrenza di "vince", "best", "chosen", "selected", "effective", "decision", "final policy" |

**Violazioni rilevate:** nessuna.

---

## Riepilogo correzioni

- **Documento 8.2.2:** due interventi minimi (§3 Priority: convenzione meccanica; §5: criterio non giustificato semanticamente).
- **Codice e test:** nessuna modifica necessaria.

---

## Dichiarazione finale

**8.2.2 conforme dopo correzioni.**

Il microstep 8.2.2 — Priority, Ranking & Weighting Model, a seguito delle correzioni minime al documento architetturale indicate in §1, risulta conforme all’audit: non introduce semantica decisionale, non viola i contratti di Fase 8 e non eredita comportamenti impliciti. Tipi, SemanticModuleOutput, ordinamento, kill-switch e test sono risultati conformi senza necessità di modifiche al codice.

---

**Fine report di audit — 8.2.2.**

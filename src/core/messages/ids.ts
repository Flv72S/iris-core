/**
 * Core Messages — ID types
 *
 * NOTE:
 * - In Core Threads, IDs are represented as `string` (see Thread.getId()).
 * - We keep the same representation here to avoid introducing new semantics.
 */

export type ThreadId = string;
export type MessageId = string;


/**
 * Phase 7 Freeze Gate — Prints the verification report to stdout.
 * Run: npm run verify:phase7 (after build)
 */
import { runVerificationAndFormat } from './phase7Verification.js';

console.log(runVerificationAndFormat());

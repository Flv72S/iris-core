generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Thread {
  id        String   @id
  title     String
  archived  Boolean
  createdAt DateTime
  updatedAt DateTime
}


// Phase 11.2.1 — Versioned contract. Breaking changes require version bump.
// Forward: Core may add optional fields. Backward: bump version on breaking change.

const String irisBridgeContractVersion = '1.0.0';

// H5 - Re-export so existing imports of the engine still work.
// Engine implementation lives in governance_registry.dart (same library as registry).

export 'governance_registry.dart' show GovernanceActivationEngine, GovernanceRegistry;
